/**
 * Tailwind size classes for the agent (thinking-indicator) icon, shared by every
 * animation strategy so the thinking-strip icon size lives in ONE place. Change
 * the size here and all animations (spin / bubbles-brain / typing-bubble) follow.
 */
export const AGENT_ICON_SIZE = 'h-9 w-9'; // 36px
