<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100" fill="none"
  stroke="currentColor" stroke-width="3.5" stroke-linecap="round" stroke-linejoin="round">
  <!-- OPTIONAL. Overrides the agent (thinking-indicator) icon shown in the chat
       thinking strip — e.g. this brain. Use `currentColor` so it inherits the
       theme. Omit this file to keep the default (the brand logo). -->
  <circle cx="50" cy="46" r="26" />
  <path d="M50 20 V72" />
  <path d="M38 38 h24 M36 54 h28" />
</svg>
